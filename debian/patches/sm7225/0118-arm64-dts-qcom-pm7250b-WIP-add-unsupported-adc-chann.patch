From: Luca Weiss <luca.weiss@fairphone.com>
Date: Sat, 13 Aug 2022 11:56:24 +0200
Subject: arm64: dts: qcom: pm7250b: WIP add unsupported adc channels

Need implementation in driver
---
 arch/arm64/boot/dts/qcom/pm7250b.dtsi | 36 +++++++++++++++++++++++++++++++++++
 1 file changed, 36 insertions(+)

diff --git a/arch/arm64/boot/dts/qcom/pm7250b.dtsi b/arch/arm64/boot/dts/qcom/pm7250b.dtsi
index e8540c3..08fc697 100644
--- a/arch/arm64/boot/dts/qcom/pm7250b.dtsi
+++ b/arch/arm64/boot/dts/qcom/pm7250b.dtsi
@@ -111,6 +111,22 @@ channel@1e {
 				label = "chg_mid";
 			};
 
+			// channel@2a {
+			// 	reg = <ADC5_BAT_THERM_30K_PU>;
+			// 	qcom,ratiometric;
+			// 	qcom,hw-settle-time = <200>;
+			// 	qcom,pre-scaling = <1 1>;
+			// 	label = "bat_therm_30k";
+			// };
+
+			// channel@4a {
+			// 	reg = <ADC5_BAT_THERM_100K_PU>;
+			// 	qcom,ratiometric;
+			// 	qcom,hw-settle-time = <200>;
+			// 	qcom,pre-scaling = <1 1>;
+			// 	label = "bat_therm";
+			// };
+
 			channel@4b {
 				reg = <ADC5_BAT_ID_100K_PU>;
 				qcom,hw-settle-time = <200>;
@@ -119,6 +135,14 @@ channel@4b {
 				label = "bat_id";
 			};
 
+			// channel@6a {
+			// 	reg = <ADC5_BAT_THERM_400K_PU>;
+			// 	qcom,ratiometric;
+			// 	qcom,hw-settle-time = <200>;
+			// 	qcom,pre-scaling = <1 1>;
+			// 	label = "bat_therm_400k";
+			// };
+
 			channel@83 {
 				reg = <ADC5_VPH_PWR>;
 				qcom,pre-scaling = <1 3>;
@@ -136,6 +160,18 @@ channel@99 {
 				qcom,pre-scaling = <1 3>;
 				label = "chg_sbux";
 			};
+
+			// channel@b0 {
+			// 	reg = <ADC5_INT_EXT_ISENSE_VBAT_VDATA>;
+			// 	qcom,pre-scaling = <1 1>;
+			// 	label = "v_i_int_vbat_vdata";
+			// };
+
+			// channel@b4 {
+			// 	reg = <ADC5_PARALLEL_ISENSE_VBAT_VDATA>;
+			// 	qcom,pre-scaling = <1 1>;
+			// 	label = "v_i_parallel_vbat_vdata";
+			// };
 		};
 
 		pm7250b_adc_tm: adc-tm@3500 {
